allDepartments = SELECT * FROM department;
addDepartment = INSERT INTO department;

allRoles = SELECT * FROM role;
addRole = INSERT INTO role;

allEmployees = SELECT * FROM employee;
addEmployee = INSERT INTO employee;
updatedEmployee = UPDATE employee.role to ? WHERE employee.id = ?